---
layout: reference
title: Typography specimen
subtitle: Heading hierarchy, prose, emphasis, and wrapping
permalink: /dev/reference/foundations/typography/
dev_only: true
reference_type: foundation
---

# Heading one — reference fixture

Normal paragraph text establishes the inherited reading measure, font stack, and line height used by this live layout. This sentence contains enough text to reveal ordinary wrapping without pretending to be lore.

## Heading two — mixed language 中文 English

**Strong text**, *emphasized text*, `inline code`, and a [local reference link]({{ '/dev/' | relative_url }}) are rendered through current global rules.

### Heading three — a deliberately longer title to test wrapping in the generated table of contents

> A block quote is a semantic Markdown element, not a bespoke fixture component.

#### Heading four

This level is included because authored documents may use a deeper hierarchy.
