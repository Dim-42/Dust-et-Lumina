---
layout: reference
title: Markdown specimen
subtitle: Semantic authored-content baseline
permalink: /dev/reference/foundations/markdown/
dev_only: true
reference_type: foundation
---

# Markdown baseline

Paragraph one uses a normal line break in source but remains one paragraph in output.

Paragraph two has a separate semantic block. It contains **strong**, *emphasis*, `code`, and a manual line break after this sentence.  
The next line is intentionally visible as a Markdown hard break.

> Quoted reference text can contain a [link]({{ '/dev/reference/foundations/links/' | relative_url }}).

---

This horizontal rule and the semantic elements above make baseline changes noticeable during future Sass work.
