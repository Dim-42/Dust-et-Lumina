---
layout: atlas
title: Atlas map fixture
subtitle: Live galaxy-map include and Three.js initialization
permalink: /dev/reference/components/map/
dev_only: true
reference_type: component
collection_type: regions
---

## Current atlas map

This is the real `atlas` layout and `galaxy-map.html` include. It loads the existing Three.js map script and live region links. The map initializer assumes the map container exists and should remain a dedicated page fixture rather than a repeatable inline component.
